module ActiveMocker

  module Collection

    class HasOneBelongTo < Base

      include Queries

      # belongs_to

      # association(force_reload = false)
      # association=(associate)
      # build_association(attributes = {})
      # create_association(attributes = {})
      # create_association!(attributes = {})
      #
      # customer
      # customer=
      # build_customer
      # create_customer
      # create_customer!

      #has_one

      # association(force_reload = false)
      # association=(associate)
      # build_association(attributes = {})
      # create_association(attributes = {})
      # create_association!(attributes = {})

      # account
      # account=
      # build_account
      # create_account
      # create_account!

    end
  end
end